import { ButtonText, Container } from './go-to-timer-button.styles';

const GoToTimer = () => {
  return (
    <Container>
      <ButtonText>Go To Timer</ButtonText>
    </Container>
  );
};

export default GoToTimer;
