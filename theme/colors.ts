const textPrimary = '#412a4c';
const textSecondary = '#827089';

const backgroundPrimary = '#fff';
const backgroundSecondary = '#fef9f7';

const playPlusButton = '#c95ebe';
const playPlusButtonHover = '#bb42ae';

const stopButton = '#e87161';
const stopButtonHover = '#df503d';

const modeButtonInactive = '#827089';
const modeButtonInactiveHover = '#5e5263';
const modeButtonActive = '#2c1338';
const modeButtonBackground = '#f1f0f2';

const black = 'black';
const boxShadow = '#e6e1e1';

export const colors = {
  textPrimary,
  textSecondary,
  backgroundPrimary,
  backgroundSecondary,
  playPlusButton,
  playPlusButtonHover,
  stopButton,
  stopButtonHover,
  modeButtonInactive,
  modeButtonInactiveHover,
  modeButtonActive,
  modeButtonBackground,
  black,
  boxShadow,
};
